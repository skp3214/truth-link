# Add these to your .env.local file (create it if it doesn't exist)

# NextAuth Secret - Generate a random 32-character string
NEXTAUTH_SECRET=your-32-character-random-secret-key-here

# MongoDB Connection (you probably already have this)
MONGODB_URI=your-mongodb-connection-string

# Resend API Key (you probably already have this)
RESEND_API_KEY=your-resend-api-key

# NextAuth URL (optional but recommended for production)
NEXTAUTH_URL=http://localhost:3000